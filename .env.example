# PostgreSQL connection (update port if needed, default: 5499)
DATABASE_URI=postgresql://postgres@127.0.0.1:5499/title_loans_db

# Generate a new secret: openssl rand -base64 32
PAYLOAD_SECRET=YOUR_SECRET_HERE_GENERATE_WITH_OPENSSL