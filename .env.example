# === DATABASE CONFIGURATION ===
# Choose ONE based on your development setup:

# Option A: Running app via docker-compose (recommended)
# Use container name 'postgres' and internal port 5432
DATABASE_URI=postgresql://postgres@postgres:5432/title_loans_db

# Option B: Running app locally on host (pnpm dev)
# Use localhost with the EXPOSED port from docker-compose.yml (the left side of ports: 'XXXX:5432')
# Start postgres first: docker-compose up postgres
# DATABASE_URI=postgresql://postgres@localhost:XXXX/title_loans_db

# Generate a new secret: openssl rand -base64 32
PAYLOAD_SECRET=YOUR_SECRET_HERE_GENERATE_WITH_OPENSSL